require('prototypes.utils.math') -- расширяет существующий тип `math`
Utils = {
    string = require('prototypes.utils.string'),
    table = require('prototypes.utils.table'),
    time = require('prototypes.utils.time')
}
